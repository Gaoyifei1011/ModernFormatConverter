<Window
    x:Class="ModernFormatConverter.Views.Dialogs.VideoConversionOutputConfigurationWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:class="using:ModernFormatConverter.Extensions.DataType.Class"
    xmlns:collection="using:System.Collections"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    xmlns:converter="using:ModernFormatConverter.Helpers.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:model="using:ModernFormatConverter.Models"
    xmlns:rootservice="using:ModernFormatConverter.Services.Root"
    xmlns:system="using:System"
    xmlns:window="using:ModernFormatConverter.Views.Windows"
    Title="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('Title')}"
    SystemBackdrop="{x:Bind WindowSystemBackdrop, Mode=OneWay}"
    mc:Ignorable="d">

    <Page
        x:Name="VideoConversionOutputConfigurationPage"
        ActualThemeChanged="{x:Bind OnActualThemeChanged}"
        FlowDirection="{x:Bind ((FrameworkElement)window:MainWindow.Current.Content).FlowDirection, Mode=OneWay}"
        Loaded="{x:Bind OnLoaded}"
        RequestedTheme="{x:Bind WindowTheme, Mode=OneWay}">

        <Page.Resources>
            <ResourceDictionary>
                <MenuFlyout x:Name="TitlebarMenuFlyout" Placement="BottomEdgeAlignedLeft">

                    <MenuFlyoutItem
                        Height="28"
                        Padding="11,0"
                        FontSize="12"
                        IsEnabled="False"
                        Text="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('Restore')}">

                        <MenuFlyoutItem.Icon>
                            <FontIcon Margin="6" Glyph="&#xE923;" />
                        </MenuFlyoutItem.Icon>
                    </MenuFlyoutItem>

                    <MenuFlyoutItem
                        Height="28"
                        Padding="11,0"
                        Click="{x:Bind OnMoveClicked}"
                        FontSize="12"
                        Tag="{x:Bind TitlebarMenuFlyout}"
                        Text="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('Move')}" />

                    <MenuFlyoutItem
                        Height="28"
                        Padding="11,0"
                        Click="{x:Bind OnSizeClicked}"
                        FontSize="12"
                        Tag="{x:Bind TitlebarMenuFlyout}"
                        Text="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('Size')}" />

                    <MenuFlyoutItem
                        Height="28"
                        Padding="11,0"
                        FontSize="12"
                        IsEnabled="False"
                        Text="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('Minimize')}">

                        <MenuFlyoutItem.Icon>
                            <FontIcon Margin="6" Glyph="&#xE921;" />
                        </MenuFlyoutItem.Icon>
                    </MenuFlyoutItem>

                    <MenuFlyoutItem
                        Height="28"
                        Padding="11,0"
                        FontSize="12"
                        IsEnabled="False"
                        Text="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('Maximize')}">

                        <MenuFlyoutItem.Icon>
                            <FontIcon Margin="6" Glyph="&#xE922;" />
                        </MenuFlyoutItem.Icon>
                    </MenuFlyoutItem>

                    <MenuFlyoutSeparator Width="200" />

                    <MenuFlyoutItem
                        Height="28"
                        Padding="11,0"
                        Click="{x:Bind OnCloseClicked}"
                        FontSize="12"
                        Text="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('Close')}">

                        <MenuFlyoutItem.Icon>
                            <FontIcon Margin="6" Glyph="&#xE8BB;" />
                        </MenuFlyoutItem.Icon>

                        <MenuFlyoutItem.KeyboardAccelerators>
                            <KeyboardAccelerator Key="F4" Modifiers="Menu" />
                        </MenuFlyoutItem.KeyboardAccelerators>
                    </MenuFlyoutItem>
                </MenuFlyout>
            </ResourceDictionary>
        </Page.Resources>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!--  视频格式转换配置标题栏  -->
            <TitleBar
                x:Name="AppTitlebar"
                Title="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('Title')}"
                Grid.Row="0"
                Height="30"
                Padding="0"
                VerticalAlignment="Top">

                <TitleBar.Resources>
                    <x:Double x:Key="TitleBarExpandedHeight">30</x:Double>
                </TitleBar.Resources>

                <TitleBar.LeftHeader>
                    <ImageIcon
                        Grid.Column="1"
                        MaxWidth="16"
                        MaxHeight="16"
                        Margin="10,0,-4,0"
                        Source="ms-appx:///Assets/ControlIcon/Settings.png" />
                </TitleBar.LeftHeader>
            </TitleBar>

            <!--  视频格式转换配置控制栏  -->
            <Grid Grid.Row="1" Margin="6,0,6,3">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <SelectorBar
                    x:Name="VideoConversionOutputConfigurationSelectorBar"
                    Grid.Column="0"
                    Margin="-8,-4,0,0"
                    Padding="0"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Center"
                    SelectedItem="{x:Bind SelectedItem, Mode=OneWay}"
                    SelectionChanged="{x:Bind OnSelectorBarSelectionChanged}">

                    <SelectorBarItem Text="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('Video')}" />
                    <SelectorBarItem Text="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('Audio')}" />
                    <SelectorBarItem Text="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('Subtitle')}" Visibility="{x:Bind GetHasSubtitleConfiguration(SelectedVideoConversionTypeKind), Mode=OneWay}" />
                </SelectorBar>

                <CommandBar
                    Grid.Column="1"
                    Height="36"
                    Margin="0,0,-10,0"
                    Padding="0"
                    VerticalAlignment="Center"
                    DefaultLabelPosition="Right">

                    <CommandBar.PrimaryCommands>
                        <AppBarButton
                            Click="{x:Bind OnOkClicked}"
                            Label="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('OK')}"
                            Style="{StaticResource AppBarButtonStyle}">

                            <AppBarButton.Icon>
                                <FontIcon Glyph="&#xE8FB;" />
                            </AppBarButton.Icon>
                        </AppBarButton>
                    </CommandBar.PrimaryCommands>
                </CommandBar>
            </Grid>

            <MenuFlyoutSeparator Grid.Row="2" Margin="6,0,6,3" />

            <ScrollViewer
                x:Name="VideoConversionOutputConfigurationScroll"
                Grid.Row="3"
                Padding="8,0,10,3"
                HorizontalScrollBarVisibility="Disabled"
                HorizontalScrollMode="Disabled"
                VerticalScrollBarVisibility="Auto"
                VerticalScrollMode="Enabled"
                ViewChanged="{x:Bind OnViewChanged}">

                <StackPanel>
                    <TextBlock
                        x:Name="VideoHeader"
                        Margin="2,0,0,5"
                        FontSize="16"
                        Text="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('Video')}" />

                    <!--  格式转换类型  -->
                    <Grid
                        Height="60"
                        Margin="0,0,0,2"
                        Padding="16,0"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="{StaticResource ControlCornerRadius}">

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBlock
                            Grid.Column="0"
                            Margin="0,0,0,2"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            Style="{StaticResource BodyTextBlockStyle}"
                            Text="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('FormatConversionType')}" />

                        <ComboBox
                            Grid.Column="1"
                            VerticalAlignment="Center"
                            DisplayMemberPath="DisplayMember"
                            ItemsSource="{x:Bind FormatConversionTypeList}"
                            SelectedItem="{x:Bind SelectedFormatConversionType, Mode=OneWay}"
                            SelectionChanged="{x:Bind OnFormatConversionTypeSelectionChanged}"
                            Style="{StaticResource ComboBoxStyle}" />
                    </Grid>

                    <!--  视频编码  -->
                    <Grid
                        Height="60"
                        Margin="0,0,0,2"
                        Padding="16,0"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="{StaticResource ControlCornerRadius}">

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBlock
                            Grid.Column="0"
                            Margin="0,0,0,2"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            Style="{StaticResource BodyTextBlockStyle}"
                            Text="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('VideoEncoding')}" />

                        <ComboBox
                            Grid.Column="1"
                            VerticalAlignment="Center"
                            DisplayMemberPath="DisplayMember"
                            ItemsSource="{x:Bind VideoEncodingCollection}"
                            SelectedItem="{x:Bind SelectedVideoEncoding, Mode=OneWay}"
                            SelectionChanged="{x:Bind OnVideoEncodingSelectionChanged}"
                            Style="{StaticResource ComboBoxStyle}" />
                    </Grid>

                    <!--  大小限制  -->
                    <Grid
                        Height="60"
                        Margin="0,0,0,2"
                        Padding="16,0"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="{StaticResource ControlCornerRadius}">

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBlock
                            Grid.Column="0"
                            Margin="0,0,0,2"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            Style="{StaticResource BodyTextBlockStyle}"
                            Text="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('SizeLimitation')}" />

                        <ComboBox
                            Grid.Column="1"
                            VerticalAlignment="Center"
                            DisplayMemberPath="DisplayMember"
                            ItemsSource="{x:Bind SizeLimitationCollection}"
                            SelectedItem="{x:Bind SelectedSizeLimitation, Mode=OneWay}"
                            SelectionChanged="{x:Bind OnSizeLimitationSelectionChanged}"
                            Style="{StaticResource ComboBoxStyle}" />
                    </Grid>

                    <!--  屏幕大小  -->
                    <Expander
                        Margin="0,0,0,2"
                        Padding="0"
                        HorizontalAlignment="Stretch"
                        HorizontalContentAlignment="Stretch"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        IsExpanded="True">

                        <Expander.Header>
                            <TextBlock
                                Grid.Column="1"
                                VerticalAlignment="Center"
                                Text="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('ScreenSize')}" />
                        </Expander.Header>

                        <StackPanel Padding="0">
                            <Grid
                                Height="52"
                                Padding="16,0"
                                HorizontalAlignment="Stretch"
                                VerticalAlignment="Center">

                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <TextBlock
                                    Grid.Column="0"
                                    Margin="0,0,0,2"
                                    HorizontalAlignment="Left"
                                    VerticalAlignment="Center"
                                    Style="{StaticResource BodyTextBlockStyle}"
                                    Text="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('SelectScreenSize')}" />

                                <ComboBox
                                    Grid.Column="1"
                                    VerticalAlignment="Center"
                                    DisplayMemberPath="DisplayMember"
                                    ItemsSource="{x:Bind ScreenSizeCollection}"
                                    SelectedItem="{x:Bind SelectedScreenSize, Mode=OneWay}"
                                    SelectionChanged="{x:Bind OnScreenSizeSelectionChanged}"
                                    Style="{StaticResource ComboBoxStyle}" />
                            </Grid>

                            <MenuFlyoutSeparator Visibility="{x:Bind GetIsCustomScreenSizeSelected(SelectedScreenSize), Mode=OneWay}" />

                            <Grid
                                Height="52"
                                Padding="16,0"
                                HorizontalAlignment="Stretch"
                                VerticalAlignment="Center"
                                Visibility="{x:Bind GetIsCustomScreenSizeSelected(SelectedScreenSize), Mode=OneWay}">

                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <TextBlock
                                    Grid.Column="0"
                                    HorizontalAlignment="Left"
                                    VerticalAlignment="Center"
                                    Text="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('CustomSize')}" />

                                <StackPanel
                                    Grid.Column="1"
                                    VerticalAlignment="Center"
                                    Orientation="Horizontal">

                                    <TextBlock
                                        HorizontalAlignment="Right"
                                        VerticalAlignment="Center"
                                        Text="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('Width')}" />

                                    <NumberBox
                                        Margin="5,0,0,0"
                                        VerticalAlignment="Center"
                                        LargeChange="1"
                                        SmallChange="1"
                                        SpinButtonPlacementMode="Inline"
                                        ValueChanged="{x:Bind OnScreenWidthValueChanged}"
                                        Value="{x:Bind ScreenWidth, Mode=OneWay}" />

                                    <TextBlock
                                        Margin="10,0,0,0"
                                        HorizontalAlignment="Right"
                                        VerticalAlignment="Center"
                                        Text="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('Height')}" />

                                    <NumberBox
                                        Margin="5,0,0,0"
                                        VerticalAlignment="Center"
                                        LargeChange="1"
                                        SmallChange="1"
                                        SpinButtonPlacementMode="Inline"
                                        ValueChanged="{x:Bind OnScreenHeightValueChanged}"
                                        Value="{x:Bind ScreenHeight, Mode=OneWay}" />
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Expander>

                    <!--  比特率  -->
                    <Grid
                        Height="60"
                        Margin="0,0,0,2"
                        Padding="16,0"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="{StaticResource ControlCornerRadius}">

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBlock
                            Grid.Column="0"
                            Margin="0,0,0,2"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            Style="{StaticResource BodyTextBlockStyle}"
                            Text="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('BitRate')}" />

                        <ComboBox
                            Grid.Column="1"
                            VerticalAlignment="Center"
                            DisplayMemberPath="DisplayMember"
                            ItemsSource="{x:Bind VideoBitRateCollection}"
                            SelectedItem="{x:Bind SelectedVideoBitRate, Mode=OneWay}"
                            SelectionChanged="{x:Bind OnVideoBitRateSelectionChanged}"
                            Style="{StaticResource ComboBoxStyle}" />
                    </Grid>

                    <!--  固定速率系数  -->
                    <Expander
                        Margin="0,0,0,2"
                        Padding="0"
                        HorizontalAlignment="Stretch"
                        HorizontalContentAlignment="Stretch"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        IsExpanded="True">

                        <Expander.Header>
                            <TextBlock
                                Grid.Column="1"
                                VerticalAlignment="Center"
                                Text="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('ConstantRatefactor')}" />
                        </Expander.Header>

                        <StackPanel Padding="0">
                            <Grid
                                Height="52"
                                Padding="16,0"
                                HorizontalAlignment="Stretch"
                                VerticalAlignment="Center">

                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <TextBlock
                                    Grid.Column="0"
                                    Margin="0,0,0,2"
                                    HorizontalAlignment="Left"
                                    VerticalAlignment="Center"
                                    Style="{StaticResource BodyTextBlockStyle}"
                                    Text="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('UseCRF')}" />

                                <ToggleSwitch
                                    Grid.Column="1"
                                    IsOn="{x:Bind UseCRF, Mode=OneWay}"
                                    OffContent="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('UseCRFOffContent')}"
                                    OnContent="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('UseCRFOnContent')}"
                                    Style="{StaticResource ToggleSwitchStyle}"
                                    Toggled="{x:Bind OnUseCRFToggled}" />
                            </Grid>

                            <MenuFlyoutSeparator Visibility="{x:Bind UseCRF, Mode=OneWay}" />

                            <Grid
                                Height="52"
                                Padding="16,0"
                                HorizontalAlignment="Stretch"
                                VerticalAlignment="Center"
                                Visibility="{x:Bind UseCRF, Mode=OneWay}">

                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <TextBlock
                                    Grid.Column="0"
                                    Margin="0,0,5,2"
                                    HorizontalAlignment="Left"
                                    VerticalAlignment="Center"
                                    Style="{StaticResource BodyTextBlockStyle}"
                                    Text="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('CRFSize')}">

                                    <ToolTipService.ToolTip>
                                        <ToolTip
                                            MaxWidth="9999"
                                            Padding="0"
                                            Background="Transparent">

                                            <Grid>
                                                <SystemBackdropElement CornerRadius="{StaticResource ControlCornerRadius}" SystemBackdrop="{StaticResource AcrylicBackgroundFillColorDefaultBackdrop}" />

                                                <Grid Padding="{StaticResource ToolTipBorderPadding}">

                                                    <Grid.RowDefinitions>
                                                        <RowDefinition Height="Auto" />
                                                        <RowDefinition Height="Auto" />
                                                    </Grid.RowDefinitions>

                                                    <TextBlock Grid.Row="0" Text="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('CRFSizeScale')}" />
                                                    <TextBlock Grid.Row="1" Text="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('CRFSizeInformation')}" />
                                                </Grid>
                                            </Grid>
                                        </ToolTip>
                                    </ToolTipService.ToolTip>
                                </TextBlock>

                                <Slider
                                    Grid.Column="2"
                                    Width="200"
                                    Margin="5,0,0,0"
                                    VerticalAlignment="Center"
                                    Maximum="50"
                                    Minimum="10"
                                    SmallChange="1"
                                    StepFrequency="1"
                                    ValueChanged="{x:Bind OnCRFValueChanged}" />
                            </Grid>
                        </StackPanel>
                    </Expander>

                    <!--  GPU  -->
                    <Grid
                        Height="60"
                        Margin="0,0,0,2"
                        Padding="16,0"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="{StaticResource ControlCornerRadius}">

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBlock
                            Grid.Column="0"
                            Margin="0,0,0,2"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            Style="{StaticResource BodyTextBlockStyle}"
                            Text="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('GPU')}" />

                        <ComboBox
                            Grid.Column="1"
                            VerticalAlignment="Center"
                            DisplayMemberPath="DisplayMember"
                            ItemsSource="{x:Bind GPUCollection}"
                            SelectedItem="{x:Bind SelectedGPU, Mode=OneWay}"
                            SelectionChanged="{x:Bind OnGPUSelectionChanged}"
                            Style="{StaticResource ComboBoxStyle}" />
                    </Grid>

                    <!--  每秒帧数  -->
                    <Grid
                        Height="60"
                        Margin="0,0,0,2"
                        Padding="16,0"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="{StaticResource ControlCornerRadius}">

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBlock
                            Grid.Column="0"
                            Margin="0,0,0,2"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            Style="{StaticResource BodyTextBlockStyle}"
                            Text="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('FramePerSecond')}" />

                        <ComboBox
                            Grid.Column="1"
                            VerticalAlignment="Center"
                            DisplayMemberPath="DisplayMember"
                            ItemsSource="{x:Bind FramePerSecondList}"
                            SelectedItem="{x:Bind SelectedFramePerSecond, Mode=OneWay}"
                            SelectionChanged="{x:Bind OnFramePerSecondSelectionChanged}"
                            Style="{StaticResource ComboBoxStyle}" />
                    </Grid>

                    <!--  宽高比  -->
                    <Grid
                        Height="60"
                        Margin="0,0,0,2"
                        Padding="16,0"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="{StaticResource ControlCornerRadius}">

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBlock
                            Grid.Column="0"
                            Margin="0,0,0,2"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            Style="{StaticResource BodyTextBlockStyle}"
                            Text="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('AspectRatio')}" />

                        <ComboBox
                            Grid.Column="1"
                            VerticalAlignment="Center"
                            DisplayMemberPath="DisplayMember"
                            ItemsSource="{x:Bind AspectRatioCollection}"
                            SelectedItem="{x:Bind SelectedAspectRatio, Mode=OneWay}"
                            SelectionChanged="{x:Bind OnAspectRatioSelectionChanged}"
                            Style="{StaticResource ComboBoxStyle}" />
                    </Grid>

                    <!--  二次编码  -->
                    <Grid
                        Grid.Row="2"
                        Height="60"
                        Margin="0,0,0,2"
                        Padding="16,0"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="{StaticResource ControlCornerRadius}">

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBlock
                            Grid.Column="0"
                            Margin="0,0,0,2"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            Style="{StaticResource BodyTextBlockStyle}"
                            Text="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('SecondaryEncoding')}" />

                        <ToggleSwitch
                            Grid.Column="1"
                            IsEnabled="{x:Bind IsSecondaryEncodingEnabled, Mode=OneWay}"
                            IsOn="{x:Bind SecondaryEncoding, Mode=OneWay}"
                            OffContent="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('SecondaryEncodingOffContent')}"
                            OnContent="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('SecondaryEncodingOnContent')}"
                            Style="{StaticResource ToggleSwitchStyle}"
                            Toggled="{x:Bind OnSecondaryEncodingToggled}" />
                    </Grid>

                    <!--  关键帧间隔  -->
                    <Grid
                        Height="60"
                        Margin="0,0,0,2"
                        Padding="16,0"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="{StaticResource ControlCornerRadius}">

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBlock
                            Grid.Column="0"
                            Margin="0,0,0,2"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            Style="{StaticResource BodyTextBlockStyle}"
                            Text="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('KeyFrameInterval')}" />

                        <ComboBox
                            Grid.Column="1"
                            VerticalAlignment="Center"
                            DisplayMemberPath="DisplayMember"
                            ItemsSource="{x:Bind KeyFrameIntervalCollection}"
                            SelectedItem="{x:Bind SelectedKeyFrameInterval, Mode=OneWay}"
                            SelectionChanged="{x:Bind OnKeyFrameIntervalSelectionChanged}"
                            Style="{StaticResource ComboBoxStyle}" />
                    </Grid>

                    <!--  反交错  -->
                    <Grid
                        Grid.Row="2"
                        Height="60"
                        Margin="0,0,0,2"
                        Padding="16,0"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="{StaticResource ControlCornerRadius}">

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBlock
                            Grid.Column="0"
                            Margin="0,0,0,2"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            Style="{StaticResource BodyTextBlockStyle}"
                            Text="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('DeInterlace')}" />

                        <ToggleSwitch
                            Grid.Column="1"
                            IsOn="{x:Bind DeInterlace, Mode=OneWay}"
                            OffContent="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('DeInterlaceOffContent')}"
                            OnContent="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('DeInterlaceOnContent')}"
                            Style="{StaticResource ToggleSwitchStyle}"
                            Toggled="{x:Bind OnDeInterlaceToggled}" />
                    </Grid>

                    <!--  旋转  -->
                    <Grid
                        Height="60"
                        Margin="0,0,0,2"
                        Padding="16,0"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="{StaticResource ControlCornerRadius}">

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBlock
                            Grid.Column="0"
                            Margin="0,0,0,2"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            Style="{StaticResource BodyTextBlockStyle}"
                            Text="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('Rotation')}" />

                        <ComboBox
                            Grid.Column="1"
                            VerticalAlignment="Center"
                            DisplayMemberPath="DisplayMember"
                            ItemsSource="{x:Bind RotationList}"
                            SelectedItem="{x:Bind SelectedRotation, Mode=OneWay}"
                            SelectionChanged="{x:Bind OnRotationSelectionChanged}"
                            Style="{StaticResource ComboBoxStyle}" />
                    </Grid>

                    <!--  镜像反转  -->
                    <Grid
                        Grid.Row="2"
                        Height="60"
                        Margin="0,0,0,2"
                        Padding="16,0"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="{StaticResource ControlCornerRadius}">

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBlock
                            Grid.Column="0"
                            Margin="0,0,0,2"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            Style="{StaticResource BodyTextBlockStyle}"
                            Text="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('MirrorReversal')}" />

                        <ToggleSwitch
                            Grid.Column="1"
                            IsOn="{x:Bind MirrorReversal, Mode=OneWay}"
                            OffContent="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('MirrorReversalOffContent')}"
                            OnContent="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('MirrorReversalOnContent')}"
                            Style="{StaticResource ToggleSwitchStyle}"
                            Toggled="{x:Bind OnMirrorReversalToggled}" />
                    </Grid>

                    <!--  淡入效果  -->
                    <Grid
                        Height="60"
                        Margin="0,0,0,2"
                        Padding="16,0"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="{StaticResource ControlCornerRadius}">

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBlock
                            Grid.Column="0"
                            Margin="0,0,0,2"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            Style="{StaticResource BodyTextBlockStyle}"
                            Text="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('FadeInEffect')}" />

                        <ComboBox
                            Grid.Column="1"
                            VerticalAlignment="Center"
                            DisplayMemberPath="DisplayMember"
                            ItemsSource="{x:Bind VideoFadeInEffectList}"
                            SelectedItem="{x:Bind SelectedVideoFadeInEffect, Mode=OneWay}"
                            SelectionChanged="{x:Bind OnVideoFadeInEffectSelectionChanged}"
                            Style="{StaticResource ComboBoxStyle}" />
                    </Grid>

                    <!--  淡出效果  -->
                    <Grid
                        Height="60"
                        Margin="0,0,0,2"
                        Padding="16,0"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="{StaticResource ControlCornerRadius}">

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBlock
                            Grid.Column="0"
                            Margin="0,0,0,2"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            Style="{StaticResource BodyTextBlockStyle}"
                            Text="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('FadeOutEffect')}" />

                        <ComboBox
                            Grid.Column="1"
                            VerticalAlignment="Center"
                            DisplayMemberPath="DisplayMember"
                            ItemsSource="{x:Bind VideoFadeOutEffectList}"
                            SelectedItem="{x:Bind SelectedVideoFadeOutEffect, Mode=OneWay}"
                            SelectionChanged="{x:Bind OnVideoFadeOutEffectSelectionChanged}"
                            Style="{StaticResource ComboBoxStyle}" />
                    </Grid>

                    <TextBlock
                        x:Name="AudioHeader"
                        Margin="2,0,0,5"
                        FontSize="16"
                        Text="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('Audio')}" />

                    <Grid
                        Height="60"
                        Margin="0,0,0,2"
                        Padding="16,0"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="{StaticResource ControlCornerRadius}"
                        Visibility="{x:Bind converter:ValueConverterHelper.BooleanToVisibilityReverseConvert(IsAudioConfigurationSupported), Mode=OneWay}">

                        <TextBlock
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Style="{StaticResource BodyTextBlockStyle}"
                            Text="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('NotSupported')}" />
                    </Grid>

                    <!--  音频编码  -->
                    <StackPanel Visibility="{x:Bind IsAudioConfigurationSupported, Mode=OneWay}">
                        <Grid
                            Height="60"
                            Margin="0,0,0,2"
                            Padding="16,0"
                            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1"
                            CornerRadius="{StaticResource ControlCornerRadius}">

                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <TextBlock
                                Grid.Column="0"
                                Margin="0,0,0,2"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                Style="{StaticResource BodyTextBlockStyle}"
                                Text="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('AudioEncoding')}" />

                            <ComboBox
                                Grid.Column="1"
                                VerticalAlignment="Center"
                                DisplayMemberPath="DisplayMember"
                                ItemsSource="{x:Bind AudioEncodingCollection}"
                                SelectedItem="{x:Bind SelectedAudioEncoding, Mode=OneWay}"
                                SelectionChanged="{x:Bind OnAudioEncodingSelectionChanged}"
                                Style="{StaticResource ComboBoxStyle}" />
                        </Grid>

                        <!--  采样率  -->
                        <Grid
                            Height="60"
                            Margin="0,0,0,2"
                            Padding="16,0"
                            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1"
                            CornerRadius="{StaticResource ControlCornerRadius}">

                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <TextBlock
                                Grid.Column="0"
                                Margin="0,0,0,2"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                Style="{StaticResource BodyTextBlockStyle}"
                                Text="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('SamplingRate')}" />

                            <ComboBox
                                Grid.Column="1"
                                VerticalAlignment="Center"
                                DisplayMemberPath="DisplayMember"
                                ItemsSource="{x:Bind SamplingRateCollection}"
                                SelectedItem="{x:Bind SelectedSamplingRate, Mode=OneWay}"
                                SelectionChanged="{x:Bind OnSamplingRateSelectionChanged}"
                                Style="{StaticResource ComboBoxStyle}" />
                        </Grid>

                        <!--  音频分辨率  -->
                        <Grid
                            Height="60"
                            Margin="0,0,0,2"
                            Padding="16,0"
                            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1"
                            CornerRadius="{StaticResource ControlCornerRadius}">

                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <TextBlock
                                Grid.Column="0"
                                Margin="0,0,0,2"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                Style="{StaticResource BodyTextBlockStyle}"
                                Text="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('BitRate')}" />

                            <ComboBox
                                Grid.Column="1"
                                VerticalAlignment="Center"
                                DisplayMemberPath="DisplayMember"
                                ItemsSource="{x:Bind AudioBitRateCollection}"
                                SelectedItem="{x:Bind SelectedAudioBitRate, Mode=OneWay}"
                                SelectionChanged="{x:Bind OnAudioBitRateSelectionChanged}"
                                Style="{StaticResource ComboBoxStyle}" />
                        </Grid>

                        <!--  音频分辨率  -->
                        <Grid
                            Height="60"
                            Margin="0,0,0,2"
                            Padding="16,0"
                            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1"
                            CornerRadius="{StaticResource ControlCornerRadius}">

                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <TextBlock
                                Grid.Column="0"
                                Margin="0,0,0,2"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                Style="{StaticResource BodyTextBlockStyle}"
                                Text="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('SoundTrack')}" />

                            <ComboBox
                                Grid.Column="1"
                                VerticalAlignment="Center"
                                DisplayMemberPath="DisplayMember"
                                ItemsSource="{x:Bind SoundTrackCollection}"
                                SelectedItem="{x:Bind SelectedSoundTrack, Mode=OneWay}"
                                SelectionChanged="{x:Bind OnSoundTrackSelectionChanged}"
                                Style="{StaticResource ComboBoxStyle}" />
                        </Grid>

                        <!--  关闭音效  -->
                        <Grid
                            Grid.Row="2"
                            Height="60"
                            Margin="0,0,0,2"
                            Padding="16,0"
                            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1"
                            CornerRadius="{StaticResource ControlCornerRadius}">

                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <TextBlock
                                Grid.Column="0"
                                Margin="0,0,0,2"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                Style="{StaticResource BodyTextBlockStyle}"
                                Text="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('CloseSoundEffect')}" />

                            <ToggleSwitch
                                Grid.Column="1"
                                IsOn="{x:Bind CloseSoundEffect, Mode=OneWay}"
                                OffContent="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('CloseSoundEffectOffContent')}"
                                OnContent="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('CloseSoundEffectOnContent')}"
                                Style="{StaticResource ToggleSwitchStyle}"
                                Toggled="{x:Bind OnCloseSoundEffectToggled}" />
                        </Grid>

                        <!--  音量  -->
                        <Grid
                            Height="60"
                            Margin="0,0,0,2"
                            Padding="16,0"
                            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1"
                            CornerRadius="{StaticResource ControlCornerRadius}">

                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <TextBlock
                                Grid.Column="0"
                                Margin="0,0,0,2"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                Style="{StaticResource BodyTextBlockStyle}"
                                Text="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('Volume')}" />

                            <ComboBox
                                Grid.Column="1"
                                VerticalAlignment="Center"
                                DisplayMemberPath="DisplayMember"
                                ItemsSource="{x:Bind VolumeCollection}"
                                SelectedItem="{x:Bind SelectedVolume, Mode=OneWay}"
                                SelectionChanged="{x:Bind OnVolumeSelectionChanged}"
                                Style="{StaticResource ComboBoxStyle}" />
                        </Grid>

                        <!--  保留所有源音频输入流  -->
                        <Grid
                            Grid.Row="2"
                            Height="60"
                            Margin="0,0,0,2"
                            Padding="16,0"
                            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1"
                            CornerRadius="{StaticResource ControlCornerRadius}">

                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <TextBlock
                                Grid.Column="0"
                                Margin="0,0,0,2"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                Style="{StaticResource BodyTextBlockStyle}"
                                Text="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('PreserveAllSourceInputStream')}" />

                            <ToggleSwitch
                                Grid.Column="1"
                                IsOn="{x:Bind PreserveAllSourceInputAudioStream, Mode=OneWay}"
                                OffContent="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('PreserveAllSourceInputStreamOffContent')}"
                                OnContent="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('PreserveAllSourceInputStreamOnContent')}"
                                Style="{StaticResource ToggleSwitchStyle}"
                                Toggled="{x:Bind OnPreserveAllSourceInputAudioStreamToggled}" />
                        </Grid>

                        <!--  淡入效果  -->
                        <Grid
                            Height="60"
                            Margin="0,0,0,2"
                            Padding="16,0"
                            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1"
                            CornerRadius="{StaticResource ControlCornerRadius}">

                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <TextBlock
                                Grid.Column="0"
                                Margin="0,0,0,2"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                Style="{StaticResource BodyTextBlockStyle}"
                                Text="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('FadeInEffect')}" />

                            <ComboBox
                                Grid.Column="1"
                                VerticalAlignment="Center"
                                DisplayMemberPath="DisplayMember"
                                ItemsSource="{x:Bind AudioFadeInEffectCollection}"
                                SelectedItem="{x:Bind SelectedAudioFadeInEffect, Mode=OneWay}"
                                SelectionChanged="{x:Bind OnAudioFadeInEffectSelectionChanged}"
                                Style="{StaticResource ComboBoxStyle}" />
                        </Grid>

                        <!--  淡出效果  -->
                        <Grid
                            Height="60"
                            Margin="0,0,0,2"
                            Padding="16,0"
                            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1"
                            CornerRadius="{StaticResource ControlCornerRadius}">

                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <TextBlock
                                Grid.Column="0"
                                Margin="0,0,0,2"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                Style="{StaticResource BodyTextBlockStyle}"
                                Text="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('FadeOutEffect')}" />

                            <ComboBox
                                Grid.Column="1"
                                VerticalAlignment="Center"
                                DisplayMemberPath="DisplayMember"
                                ItemsSource="{x:Bind AudioFadeOutEffectCollection}"
                                SelectedItem="{x:Bind SelectedAudioFadeOutEffect, Mode=OneWay}"
                                SelectionChanged="{x:Bind OnAudioFadeOutEffectSelectionChanged}"
                                Style="{StaticResource ComboBoxStyle}" />
                        </Grid>

                        <!--  回声  -->
                        <Grid
                            Grid.Row="2"
                            Height="60"
                            Margin="0,0,0,2"
                            Padding="16,0"
                            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1"
                            CornerRadius="{StaticResource ControlCornerRadius}">

                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <TextBlock
                                Grid.Column="0"
                                Margin="0,0,0,2"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                Style="{StaticResource BodyTextBlockStyle}"
                                Text="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('Echo')}" />

                            <ToggleSwitch
                                Grid.Column="1"
                                IsOn="{x:Bind Echo, Mode=OneWay}"
                                OffContent="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('EchoOffContent')}"
                                OnContent="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('EchoOnContent')}"
                                Style="{StaticResource ToggleSwitchStyle}"
                                Toggled="{x:Bind OnEchoToggled}" />
                        </Grid>

                        <!--  降噪  -->
                        <Grid
                            Grid.Row="2"
                            Height="60"
                            Margin="0,0,0,2"
                            Padding="16,0"
                            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1"
                            CornerRadius="{StaticResource ControlCornerRadius}">

                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <TextBlock
                                Grid.Column="0"
                                Margin="0,0,0,2"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                Style="{StaticResource BodyTextBlockStyle}"
                                Text="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('DeNoise')}" />

                            <ToggleSwitch
                                Grid.Column="1"
                                IsOn="{x:Bind DeNoise, Mode=OneWay}"
                                OffContent="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('DeNoiseOffContent')}"
                                OnContent="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('DeNoiseOnContent')}"
                                Style="{StaticResource ToggleSwitchStyle}"
                                Toggled="{x:Bind OnDeNoiseToggled}" />
                        </Grid>

                        <!--  反向  -->
                        <Grid
                            Grid.Row="2"
                            Height="60"
                            Margin="0,0,0,2"
                            Padding="16,0"
                            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1"
                            CornerRadius="{StaticResource ControlCornerRadius}">

                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <TextBlock
                                Grid.Column="0"
                                Margin="0,0,0,2"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                Style="{StaticResource BodyTextBlockStyle}"
                                Text="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('Reverse')}" />

                            <ToggleSwitch
                                Grid.Column="1"
                                IsOn="{x:Bind Reverse, Mode=OneWay}"
                                OffContent="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('ReverseOffContent')}"
                                OnContent="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('ReverseOnContent')}"
                                Style="{StaticResource ToggleSwitchStyle}"
                                Toggled="{x:Bind OnReverseToggled}" />
                        </Grid>
                    </StackPanel>

                    <TextBlock
                        x:Name="SubtitleHeader"
                        Margin="2,0,0,5"
                        FontSize="16"
                        Text="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('Subtitle')}"
                        Visibility="{x:Bind GetHasSubtitleConfiguration(SelectedVideoConversionTypeKind), Mode=OneWay}" />

                    <!--  保留所有源输入流  -->
                    <Grid
                        Grid.Row="2"
                        Height="60"
                        Margin="0,0,0,2"
                        Padding="16,0"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="{StaticResource ControlCornerRadius}"
                        Visibility="{x:Bind GetHasSubtitleConfiguration(SelectedVideoConversionTypeKind), Mode=OneWay}">

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBlock
                            Grid.Column="0"
                            Margin="0,0,0,2"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            Style="{StaticResource BodyTextBlockStyle}"
                            Text="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('PreserveAllSourceInputStream')}" />

                        <ToggleSwitch
                            Grid.Column="1"
                            IsEnabled="{x:Bind IsPreserveAllSourceInputSubtitleStreamEnabled, Mode=OneWay}"
                            IsOn="{x:Bind PreserveAllSourceInputSubtitleStream, Mode=OneWay}"
                            OffContent="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('PreserveAllSourceInputStreamOffContent')}"
                            OnContent="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('PreserveAllSourceInputStreamOnContent')}"
                            Style="{StaticResource ToggleSwitchStyle}"
                            Toggled="{x:Bind OnPreserveAllSourceInputSubtitleStreamToggled}" />
                    </Grid>

                    <!--  附加字幕  -->
                    <Grid
                        Grid.Row="6"
                        Height="60"
                        Margin="0,0,0,2"
                        Padding="16,0,22,0"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="{StaticResource ControlCornerRadius}"
                        Visibility="{x:Bind GetHasSubtitleConfiguration(SelectedVideoConversionTypeKind), Mode=OneWay}">

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <StackPanel
                            Grid.Column="0"
                            VerticalAlignment="Center"
                            Orientation="Vertical">

                            <TextBlock
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                Style="{StaticResource BodyTextBlockStyle}"
                                Text="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('AdditionalSubtitle')}" />

                            <TextBlock
                                Margin="0,0,5,0"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                TextTrimming="CharacterEllipsis"
                                TextWrapping="NoWrap"
                                Visibility="{x:Bind CheckAdditionalSubtitlePath(AdditionalSubtitlePath), Mode=OneWay}">

                                <Hyperlink Click="{x:Bind OnAdditionalSubtitlePathClicked}" UnderlineStyle="None">
                                    <Run Text="{x:Bind AdditionalSubtitlePath, Mode=OneWay}" />
                                </Hyperlink>

                                <ToolTipService.ToolTip>
                                    <ToolTip
                                        MaxWidth="9999"
                                        Padding="0"
                                        Background="Transparent">

                                        <Grid>
                                            <SystemBackdropElement CornerRadius="{StaticResource ControlCornerRadius}" SystemBackdrop="{StaticResource AcrylicBackgroundFillColorDefaultBackdrop}" />

                                            <StackPanel Padding="{StaticResource ToolTipBorderPadding}" HorizontalAlignment="Stretch">
                                                <TextBlock Text="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('AdditionalSubtitlePath')}" />
                                                <TextBlock Text="{x:Bind AdditionalSubtitlePath, Mode=OneWay}" />
                                                <MenuFlyoutSeparator Margin="4,3" />
                                                <TextBlock Text="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('OpenFolder')}" />
                                            </StackPanel>
                                        </Grid>
                                    </ToolTip>
                                </ToolTipService.ToolTip>
                            </TextBlock>
                        </StackPanel>

                        <Button
                            Grid.Column="1"
                            Width="32"
                            Height="32"
                            Margin="5,0,-5,0"
                            Padding="0"
                            Click="{x:Bind OnSelectAdditionalSubtitleClicked}"
                            CornerRadius="{StaticResource ControlCornerRadius}">

                            <FontIcon FontSize="14" Glyph="&#xE838;" />

                            <ToolTipService.ToolTip>
                                <ToolTip
                                    MaxWidth="9999"
                                    Padding="0"
                                    Background="Transparent">

                                    <Grid>
                                        <SystemBackdropElement CornerRadius="{StaticResource ControlCornerRadius}" SystemBackdrop="{StaticResource AcrylicBackgroundFillColorDefaultBackdrop}" />
                                        <TextBlock Padding="{StaticResource ToolTipBorderPadding}" Text="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('SelectAdditionalSubtitle')}" />
                                    </Grid>
                                </ToolTip>
                            </ToolTipService.ToolTip>
                        </Button>
                    </Grid>

                    <!--  字幕嵌入类型  -->
                    <Grid
                        Height="60"
                        Margin="0,0,0,2"
                        Padding="16,0"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="{StaticResource ControlCornerRadius}"
                        Visibility="{x:Bind GetHasSubtitleConfiguration(SelectedVideoConversionTypeKind), Mode=OneWay}">

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBlock
                            Grid.Column="0"
                            Margin="0,0,0,2"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            Style="{StaticResource BodyTextBlockStyle}"
                            Text="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('SubtitleNestType')}" />

                        <ComboBox
                            Grid.Column="1"
                            VerticalAlignment="Center"
                            DisplayMemberPath="DisplayMember"
                            ItemsSource="{x:Bind SubtitleNestTypeList}"
                            SelectedItem="{x:Bind SelectedSubtitleNestType, Mode=OneWay}"
                            SelectionChanged="{x:Bind OnSubtitleNestTypeSelectionChanged}"
                            Style="{StaticResource ComboBoxStyle}" />
                    </Grid>

                    <!--  字体名称  -->
                    <Grid
                        Height="60"
                        Margin="0,0,0,2"
                        Padding="16,0"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="{StaticResource ControlCornerRadius}"
                        Visibility="{x:Bind GetHasSubtitleConfiguration(SelectedVideoConversionTypeKind), Mode=OneWay}">

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBlock
                            Grid.Column="0"
                            Margin="0,0,0,2"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            Style="{StaticResource BodyTextBlockStyle}"
                            Text="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('FontName')}" />

                        <SplitButton
                            Grid.Column="1"
                            VerticalAlignment="Center"
                            Content="{x:Bind FontName, Mode=OneWay}">

                            <SplitButton.Flyout>
                                <Flyout
                                    x:Name="FontNameFlyout"
                                    Opening="{x:Bind OnFontNameFlyoutOpening}"
                                    Placement="Bottom"
                                    ShouldConstrainToRootBounds="False">

                                    <Flyout.FlyoutPresenterStyle>
                                        <Style BasedOn="{StaticResource DefaultFlyoutPresenterStyle}" TargetType="FlyoutPresenter">
                                            <Setter Property="Padding" Value="0" />
                                            <Setter Property="MinHeight" Value="0" />
                                            <Setter Property="Background" Value="Transparent" />
                                        </Style>
                                    </Flyout.FlyoutPresenterStyle>

                                    <Grid>
                                        <SystemBackdropElement CornerRadius="{StaticResource OverlayCornerRadius}" SystemBackdrop="{StaticResource AcrylicBackgroundFillColorDefaultBackdrop}" />

                                        <Grid Padding="12,5">
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto" />
                                                <RowDefinition Height="Auto" />
                                                <RowDefinition Height="Auto" />
                                            </Grid.RowDefinitions>

                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="Auto" />
                                                    <ColumnDefinition Width="*" />
                                                </Grid.ColumnDefinitions>

                                                <ComboBox
                                                    Grid.Column="0"
                                                    VerticalAlignment="Center"
                                                    DisplayMemberPath="DisplayMember"
                                                    ItemsSource="{x:Bind FontNameList}"
                                                    SelectedItem="{x:Bind SelectedFontName, Mode=OneWay}"
                                                    SelectionChanged="{x:Bind OnFontNameSelectionChanged}"
                                                    Style="{StaticResource ComboBoxStyle}" />

                                                <AppBarSeparator Grid.Column="1" Margin="5,0" />

                                                <TextBlock
                                                    Grid.Column="2"
                                                    HorizontalAlignment="Center"
                                                    VerticalAlignment="Center"
                                                    FontFamily="{x:Bind SelectedFontFamily, Mode=OneWay}"
                                                    FontSize="16"
                                                    Text="AaXxYyZz" />
                                            </Grid>

                                            <MenuFlyoutSeparator Grid.Row="1" Margin="0,5" />

                                            <Grid
                                                Grid.Row="2"
                                                Margin="0,0,0,5"
                                                ColumnSpacing="5">

                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="*" />
                                                </Grid.ColumnDefinitions>

                                                <Button
                                                    Grid.Column="0"
                                                    HorizontalAlignment="Stretch"
                                                    Click="{x:Bind OnFontNameOkClicked}"
                                                    Content="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('OK')}"
                                                    Style="{StaticResource AccentButtonStyle}" />

                                                <Button
                                                    Grid.Column="1"
                                                    HorizontalAlignment="Stretch"
                                                    Click="{x:Bind OnFontNameCancelClicked}"
                                                    Content="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('Cancel')}" />
                                            </Grid>
                                        </Grid>
                                    </Grid>
                                </Flyout>
                            </SplitButton.Flyout>
                        </SplitButton>
                    </Grid>

                    <!--  字体大小  -->
                    <Grid
                        Height="60"
                        Margin="0,0,0,2"
                        Padding="16,0"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="{StaticResource ControlCornerRadius}"
                        Visibility="{x:Bind GetHasSubtitleConfiguration(SelectedVideoConversionTypeKind), Mode=OneWay}">

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBlock
                            Grid.Column="0"
                            Margin="0,0,0,2"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            Style="{StaticResource BodyTextBlockStyle}"
                            Text="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('FontSize')}" />

                        <ComboBox
                            Grid.Column="1"
                            VerticalAlignment="Center"
                            DisplayMemberPath="DisplayMember"
                            ItemsSource="{x:Bind FontSizeList}"
                            SelectedItem="{x:Bind SelectedFontSize, Mode=OneWay}"
                            SelectionChanged="{x:Bind OnFontSizeSelectionChanged}"
                            Style="{StaticResource ComboBoxStyle}" />
                    </Grid>

                    <!--  字体颜色  -->
                    <Grid
                        Height="60"
                        Margin="0,0,0,2"
                        Padding="16,0"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="{StaticResource ControlCornerRadius}"
                        Visibility="{x:Bind GetHasSubtitleConfiguration(SelectedVideoConversionTypeKind), Mode=OneWay}">

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBlock
                            Grid.Column="0"
                            Margin="0,0,0,2"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            Style="{StaticResource BodyTextBlockStyle}"
                            Text="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('FontColor')}" />

                        <SplitButton
                            Grid.Column="1"
                            Margin="5,0,0,0"
                            Padding="0"
                            VerticalAlignment="Center">

                            <Border
                                Width="32"
                                Height="32"
                                CornerRadius="4,0,0,4">

                                <Border.Background>
                                    <SolidColorBrush Color="{x:Bind FontColor, Mode=OneWay}" />
                                </Border.Background>
                            </Border>

                            <SplitButton.Flyout>
                                <Flyout
                                    x:Name="FontColorFlyout"
                                    Opening="{x:Bind OnFontColorFlyoutOpening}"
                                    Placement="Bottom"
                                    ShouldConstrainToRootBounds="False">

                                    <Flyout.FlyoutPresenterStyle>
                                        <Style BasedOn="{StaticResource DefaultFlyoutPresenterStyle}" TargetType="FlyoutPresenter">
                                            <Setter Property="Padding" Value="0" />
                                            <Setter Property="MinHeight" Value="0" />
                                            <Setter Property="Background" Value="Transparent" />
                                        </Style>
                                    </Flyout.FlyoutPresenterStyle>

                                    <Grid>
                                        <SystemBackdropElement CornerRadius="{StaticResource OverlayCornerRadius}" SystemBackdrop="{StaticResource AcrylicBackgroundFillColorDefaultBackdrop}" />

                                        <Grid Padding="12,5">
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto" />
                                                <RowDefinition Height="Auto" />
                                                <RowDefinition Height="Auto" />
                                            </Grid.RowDefinitions>

                                            <ColorPicker Grid.Row="0" Color="{x:Bind SelectedFontColor, Mode=TwoWay}" />

                                            <MenuFlyoutSeparator Grid.Row="1" Margin="0,0,0,10" />

                                            <Grid
                                                Grid.Row="2"
                                                Margin="0,0,0,5"
                                                ColumnSpacing="5">

                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="*" />
                                                </Grid.ColumnDefinitions>

                                                <Button
                                                    Grid.Column="0"
                                                    HorizontalAlignment="Stretch"
                                                    Click="{x:Bind OnFontColorOkClicked}"
                                                    Content="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('OK')}"
                                                    Style="{StaticResource AccentButtonStyle}" />

                                                <Button
                                                    Grid.Column="1"
                                                    HorizontalAlignment="Stretch"
                                                    Click="{x:Bind OnFontColorRestoreDefaultClicked}"
                                                    Content="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('RestoreDefault')}" />

                                                <Button
                                                    Grid.Column="2"
                                                    HorizontalAlignment="Stretch"
                                                    Click="{x:Bind OnFontColorCancelClicked}"
                                                    Content="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('Cancel')}" />
                                            </Grid>
                                        </Grid>
                                    </Grid>
                                </Flyout>
                            </SplitButton.Flyout>
                        </SplitButton>
                    </Grid>

                    <!--  字体边框风格  -->
                    <Grid
                        Height="60"
                        Margin="0,0,0,2"
                        Padding="16,0"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="{StaticResource ControlCornerRadius}"
                        Visibility="{x:Bind GetHasSubtitleConfiguration(SelectedVideoConversionTypeKind), Mode=OneWay}">

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBlock
                            Grid.Column="0"
                            Margin="0,0,0,2"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            Style="{StaticResource BodyTextBlockStyle}"
                            Text="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('FontBorderStyle')}" />

                        <ComboBox
                            Grid.Column="1"
                            VerticalAlignment="Center"
                            DisplayMemberPath="DisplayMember"
                            ItemsSource="{x:Bind FontBorderStyleList}"
                            SelectedItem="{x:Bind SelectedFontBorderStyle, Mode=OneWay}"
                            SelectionChanged="{x:Bind OnFontBorderStyleSelectionChanged}"
                            Style="{StaticResource ComboBoxStyle}" />
                    </Grid>

                    <!--  轮廓线大小  -->
                    <Grid
                        Height="60"
                        Margin="0,0,0,2"
                        Padding="16,0"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="{StaticResource ControlCornerRadius}"
                        Visibility="{x:Bind GetHasSubtitleConfiguration(SelectedVideoConversionTypeKind), Mode=OneWay}">

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBlock
                            Grid.Column="0"
                            Margin="0,0,0,2"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            Style="{StaticResource BodyTextBlockStyle}"
                            Text="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('CounterLineSize')}" />

                        <ComboBox
                            Grid.Column="1"
                            VerticalAlignment="Center"
                            DisplayMemberPath="DisplayMember"
                            ItemsSource="{x:Bind CounterLineSizeList}"
                            SelectedItem="{x:Bind SelectedCounterLineSize, Mode=OneWay}"
                            SelectionChanged="{x:Bind OnCounterLineSizeSelectionChanged}"
                            Style="{StaticResource ComboBoxStyle}" />
                    </Grid>

                    <!--  轮廓线颜色  -->
                    <Grid
                        Height="60"
                        Margin="0,0,0,2"
                        Padding="16,0"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="{StaticResource ControlCornerRadius}"
                        Visibility="{x:Bind GetHasSubtitleConfiguration(SelectedVideoConversionTypeKind), Mode=OneWay}">

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBlock
                            Grid.Column="0"
                            Margin="0,0,0,2"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            Style="{StaticResource BodyTextBlockStyle}"
                            Text="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('CounterLineColor')}" />

                        <SplitButton
                            Grid.Column="1"
                            Margin="5,0,0,0"
                            Padding="0"
                            VerticalAlignment="Center">

                            <Border
                                Width="32"
                                Height="32"
                                CornerRadius="4,0,0,4">

                                <Border.Background>
                                    <SolidColorBrush Color="{x:Bind CounterLineColor, Mode=OneWay}" />
                                </Border.Background>
                            </Border>

                            <SplitButton.Flyout>
                                <Flyout
                                    x:Name="CounterLineColorFlyout"
                                    Opening="{x:Bind OnCounterLineColorFlyoutOpening}"
                                    Placement="Bottom"
                                    ShouldConstrainToRootBounds="False">

                                    <Flyout.FlyoutPresenterStyle>
                                        <Style BasedOn="{StaticResource DefaultFlyoutPresenterStyle}" TargetType="FlyoutPresenter">
                                            <Setter Property="Padding" Value="0" />
                                            <Setter Property="MinHeight" Value="0" />
                                            <Setter Property="Background" Value="Transparent" />
                                        </Style>
                                    </Flyout.FlyoutPresenterStyle>

                                    <Grid>
                                        <SystemBackdropElement CornerRadius="{StaticResource OverlayCornerRadius}" SystemBackdrop="{StaticResource AcrylicBackgroundFillColorDefaultBackdrop}" />

                                        <Grid Padding="12,5">
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto" />
                                                <RowDefinition Height="Auto" />
                                                <RowDefinition Height="Auto" />
                                            </Grid.RowDefinitions>

                                            <ColorPicker Grid.Row="0" Color="{x:Bind SelectedCounterLineColor, Mode=TwoWay}" />

                                            <MenuFlyoutSeparator Grid.Row="1" Margin="0,0,0,10" />

                                            <Grid
                                                Grid.Row="2"
                                                Margin="0,0,0,5"
                                                ColumnSpacing="5">

                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="*" />
                                                </Grid.ColumnDefinitions>

                                                <Button
                                                    Grid.Column="0"
                                                    HorizontalAlignment="Stretch"
                                                    Click="{x:Bind OnCounterLineColorOkClicked}"
                                                    Content="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('OK')}"
                                                    Style="{StaticResource AccentButtonStyle}" />

                                                <Button
                                                    Grid.Column="1"
                                                    HorizontalAlignment="Stretch"
                                                    Click="{x:Bind OnCounterLineColorRestoreDefaultClicked}"
                                                    Content="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('RestoreDefault')}" />

                                                <Button
                                                    Grid.Column="2"
                                                    HorizontalAlignment="Stretch"
                                                    Click="{x:Bind OnCounterLineColorCancelClicked}"
                                                    Content="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('Cancel')}" />
                                            </Grid>
                                        </Grid>
                                    </Grid>
                                </Flyout>
                            </SplitButton.Flyout>
                        </SplitButton>
                    </Grid>

                    <!--  阴影大小  -->
                    <Grid
                        Height="60"
                        Margin="0,0,0,2"
                        Padding="16,0"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="{StaticResource ControlCornerRadius}"
                        Visibility="{x:Bind GetHasSubtitleConfiguration(SelectedVideoConversionTypeKind), Mode=OneWay}">

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBlock
                            Grid.Column="0"
                            Margin="0,0,0,2"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            Style="{StaticResource BodyTextBlockStyle}"
                            Text="{x:Bind rootservice:ResourceService.VideoConversionOutputConfigurationResource.GetString('ShadowSize')}" />

                        <ComboBox
                            Grid.Column="1"
                            VerticalAlignment="Center"
                            DisplayMemberPath="DisplayMember"
                            ItemsSource="{x:Bind ShadowSizeList}"
                            SelectedItem="{x:Bind SelectedShadowSize, Mode=OneWay}"
                            SelectionChanged="{x:Bind OnShadowSizeSelectionChanged}"
                            Style="{StaticResource ComboBoxStyle}" />
                    </Grid>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Page>
</Window>
